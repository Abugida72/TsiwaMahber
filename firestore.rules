rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All authenticated users can read all data
    match /{document=**} {
      allow read: if request.auth != null;
    }

    // Users: allow unauthenticated reads so phone+code login can query,
    // but only authenticated users can write.
    match /users/{userId} {
      allow read: if true;
      allow create: if request.auth != null;
      allow update, delete: if request.auth != null;
    }

    // Areas and all sub-collections
    match /areas/{areaId} {
      allow write: if request.auth != null;

      match /{subcollection}/{docId} {
        allow write: if request.auth != null;
      }

      match /{sub1}/{docId1}/{sub2}/{docId2} {
        allow write: if request.auth != null;
      }
    }

    // Developers collection
    match /developers/{email} {
      allow write: if request.auth != null;
    }
  }
}
